[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # These are intentionally public or expected to appear in bundled output.
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_SUPABASE_URL,NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,NEXTAUTH_URL"
  SECRETS_SCAN_OMIT_PATHS = ".env.example"
